DEEPSORT:
  REID_CKPT: "./trackers/deep_sort/deep/checkpoint/ckpt.t7"
  MAX_DIST: 0.2   # 特征向量之间的最大距离，如果小于MAX_DIST的则认为是同一个
  MIN_CONFIDENCE: 0.3
  NMS_MAX_OVERLAP: 0.5
  MAX_IOU_DISTANCE: 0.7 # 边框之间的最大iou距离阈值，iou距离=1-iou，如果两个框iou>(1-MAX_IOU_DISTANCE),则认为是同一个人
  MAX_AGE: 70
  N_INIT: 3
  NN_BUDGET: 100
  
